<div class="container p-3">
    <div class="card shadow-sm">
        <div class="card-body">
            <div class="row">
                <div class="col-12 text-center">
                    <div class="mb-3">
                        <h6 class="text-muted small">Estado del pedido</h6>
                        <h5 id="status"></h5>
                    </div>
                    <div class="mb-4">
                        <h6 class="text-muted small">Número del pedido</h6>
                        <h5 id="id"></h5>
                    </div>
                    <a href="/" class="btn btn-primary text-white mb-5">Volver al inicio</a>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    let searchParams = new URLSearchParams(window.location.search);

    document.getElementById("status").innerText = searchParams.get("status").toUpperCase();
    document.getElementById("id").innerText = searchParams.get("payment_id");

</script>