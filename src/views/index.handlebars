<div class="container p-3">
    <div class="row mt-3 mb-5">
        <div class="col text-center">
            <h4>Smartphones</h4>
            <h6>Las mejores marcas al mejor precio</h6>
        </div>
    </div>
    <div class="row row-cols-lg-4 g-3">
        
        {{#each products}}

        <div class="col-12">
            <div class="card shadow-sm">
                <img id="image" title={{image}} class="card-img-top" alt="">
                <div class="card-body text-center">
                    <h5 class="card-title">{{name}}</h5>
                    <h6 class="card-subtitle">${{unit_price}}</h6>
                    <div class="mt-3">
                    <a href="/product-details/{{id}}" class="btn btn-primary text-white">Más info</a>
                    </div>
                </div>
            </div>
        </div>

        {{/each}}
    </div>
</div>

{{> security view="home"}}
